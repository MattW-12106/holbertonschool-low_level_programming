#include "main.h"

/**
 * _isdigit - A function that checks for a digit
 * @c: The variable being checked
 *
 * Return: On 0 or 1 (success)
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
return (0);
}
