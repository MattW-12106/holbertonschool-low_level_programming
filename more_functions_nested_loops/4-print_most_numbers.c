#include "main.h"

/**
 * print_most_numbers - Prints most numbers out of a sequence
 *
 * Return: On void
 */

void print_most_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
	if (i != 2 && 1 != 4)
	{
		_putchar(i + '0');
	}
	}
	_putchar('\n');
}
